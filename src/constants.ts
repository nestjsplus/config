import Debug from 'debug';

export const CONFIG_OPTIONS = 'CONFIG_OPTIONS';

export const dbg = {
  cfg: Debug('cfg'),
  trace: Debug('trace'),
};

export const NO_ENV_FILE_ERROR = 'NO-ENV-FILE-ERROR';
